listener {
  timeout = 300
  on-timeout = hyprctl dispatch 'hl.dsp.dpms({ action = "disable", monitor = "HDMI-A-1" })' 
	on-resume = hyprctl dispatch 'hl.dsp.dpms({ action = "enable", monitor = "HDMI-A-1" })'
}
